import React from 'react';

const EditableInput = ({ initialValue, onSave, label = null, placeholder = 'Write your value', emptyMsg = 'Input is empty' }) => {
    return 
        <div>
            hello
        </div>
};

export default EditableInput;
