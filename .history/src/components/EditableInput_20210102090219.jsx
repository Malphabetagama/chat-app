import React from 'react';

const EditableInput = ({ initialValue, onSave, label = null, placeholder = "" }) => {
    return 
        <div>
            hello
        </div>
};

export default EditableInput;
