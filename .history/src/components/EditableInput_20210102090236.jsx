import React from 'react';

const EditableInput = ({ initialValue, onSave, label = null, placeholder = "Write your value" }) => {
    return 
        <div>
            hello
        </div>
};

export default EditableInput;
